import React from 'react';

const Header = (): JSX.Element => {
    return (
        <header className='header'>
            <h1 className='header__title'>MOBX TODO LIST</h1>
        </header>
    );
};

export default Header;